//************************ While Loop ********************************

let index = 0
while (index <= 10) {
  // console.log(`Value of index is ${index}`);
    index = index + 2;
  }
  
let myArray = ["flash", "thor", "hulk", "superman"]
  let arr = 0

while (arr < myArray.length) {
  // console.log(`Value of index is ${arr} and ${myArray[arr]}`)
  arr++
}

let score = 11

do { //? first one execution is must happen here.
  console.log (`Score is ${score}`);
  score++
} while (score <= 10)

